import {useState}from'react';import{useNavigate}from'react-router-dom';import crest from'../assets/fordridge-crest.jpeg';import{isSupabaseConfigured,supabase}from'../lib/supabase'
export default function PortalLogin(){const n=useNavigate(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setError('');if(!isSupabaseConfigured){setError('Portal authentication is not configured yet. Add the Supabase environment values to continue.');return}setLoading(true);const{error:err}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(err)setError(err.message);else n('/portal')}return <main className="portal-login"><section className="login-brand"><div className="brand-top"><img src={crest} alt="Fordridge crest"/><span>Fordridge Schools</span></div><div><p className="eyebrow">School portal</p><h1>Built for every<br/>learning journey.</h1><p className="brand-copy">One secure home for Fordridge Junior &amp; Senior School families, learners and staff.</p></div><p className="motto">Learn <i/> Excel <i/> Achieve</p></section><section className="login-panel"><div className="login-card"><a className="back" href="/">← Public website</a><h2>Welcome back</h2><p className="muted">Sign in to access your Fordridge portal.</p><form onSubmit={submit}><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/></label>{error&&<p className="form-error">{error}</p>}<button disabled={loading}>{loading?'Signing in…':'Sign in to portal'}</button></form>{!isSupabaseConfigured&&<p className="setup-note">Development mode: connect Supabase to enable sign-in.</p>}</div></section></main>}
